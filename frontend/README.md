# Frontend

React + Vite frontend for the Visitor Pass Management System.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

The API client is configured in `src/services/api.js` and currently points to `http://localhost:5000/api`.
